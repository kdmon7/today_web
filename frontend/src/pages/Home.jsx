import React from 'react';

import HeaderBanner from '../components/banner/banner';

const Home = (props) => {
    return(
        <div id="main-wrapper">
            <div id="page-wrapper">
                <HeaderBanner/>
            </div>
        </div>
    );
}

export default Home;